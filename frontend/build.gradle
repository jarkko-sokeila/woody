
plugins {
    id 'base'
    id "com.moowork.node" version "1.3.1"
}

repositories {
    mavenCentral()
}

task buildFrontend(type: NpmTask) {
    args = ['run', 'build']
}

task testFrontend(type: NpmTask) {
    args = ['test']
}

task start(type: NpmTask) {
    args = ['run', 'start']
}

buildFrontend.dependsOn(npmSetup)
assemble.dependsOn(buildFrontend)